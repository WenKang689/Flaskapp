<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ product_details.product_name }}</title>
    <!-- Swiper CSS -->
    <link rel="stylesheet" href="https://unpkg.com/swiper/swiper-bundle.min.css" />
    <!-- Font Awesome CSS for star icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        .swiper-container {
            width: 100%;
            height: 400px;
        }
        .review {
            border-bottom: 1px solid #ccc;
            padding: 10px 0;
        }
        .rating {
            color: gold;
        }
        .fa-star {
            color: #ccc;
        }
        .fa-star.checked {
            color: #ffcc00;
        }
    </style>
</head>
<body>
    <h1>{{ product_details.product_name }}</h1>
    {% if product_details.images %}
        <!-- Swiper -->
        <div class="swiper-container">
            <div class="swiper-wrapper">
                {% for image_url in product_details.images %}
                    <div class="swiper-slide">
                        <img src="{{ image_url }}" alt="{{ product_details.product_name }} picture" style="width: 100%; height: 100%; object-fit: contain;">
                    </div>
                {% endfor %}
            </div>
            <!-- Add Pagination -->
            <div class="swiper-pagination"></div>
            <!-- Add Navigation -->
            <div class="swiper-button-next"></div>
            <div class="swiper-button-prev"></div>
        </div>
    {% else %}
        <p>No images available for this product.</p>
    {% endif %}
    <!-- Display other product details -->
    <p>Brand: {{ product_details.brand }}</p>
    <p>Processor: {{ product_details.processor }}</p>
    <p>Graphics: {{ product_details.graphics }}</p>
    <p>Dimensions: {{ product_details.dimensions }}</p>
    <p>Weight: {{ product_details.weight }}</p>
    <p>OS: {{ product_details.os }}</p>
    <p>Memory: {{ product_details.memory }}</p>
    <p>Storage: {{ product_details.storage }}</p>
    <p>Power Supply: {{ product_details.power_supply }}</p>
    <p>Battery: {{ product_details.battery }}</p>
    <p>Price: {{ product_details.price }}</p>
    <p><span class="{{ 'out-of-stock' if product_details.stock_status == 'Out of Stock' else '' }}">{{ product_details.stock_status }}</span></p>

    <!-- Product Reviews -->
    <div>
        <h2>Product Reviews</h2>
        {% if product_details.reviews %}
            <ul>
                {% for review in product_details.reviews %}
                    <li class="review">
                        <strong>User: {{ review.username }}</strong><br>
                        Rating: 
                        {% for i in range(1, 6) %}
                            <span class="fa fa-star {% if i <= review.rating %} checked {% endif %}"></span>
                        {% endfor %}
                        <br>
                        Review: {{ review.review }}
                        <br>
                        Date: {{ review.date }}
                        <br>
                        {% if 'reply' in review %}
                            <p>Laptop Kaw Kaw's Response: {{ review.reply }}</p>
                        {% endif %}
                    </li>
                {% endfor %}
            </ul>
        {% else %}
            <p>No reviews yet.</p>
        {% endif %}
    </div>

    <!-- Swiper JS -->
    <script src="https://unpkg.com/swiper/swiper-bundle.min.js"></script>

    <!-- Swiper JS initialization -->
    <script>
        document.addEventListener("DOMContentLoaded", function() {
            var swiper = new Swiper('.swiper-container', {
                loop: true,
                pagination: {
                    el: '.swiper-pagination',
                    clickable: true,
                },
                navigation: {
                    nextEl: '.swiper-button-next',
                    prevEl: '.swiper-button-prev',
                },
            });
        });
    </script>
</body>
</html>
